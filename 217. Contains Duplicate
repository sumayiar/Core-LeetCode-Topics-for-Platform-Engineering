class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        hash = set()

        for n in nums:
            if n in hash:
                return True
            hash.add(n)
        return False

#sets store unique values
#time and space complexity is O(n) because each lookup is constant time.
